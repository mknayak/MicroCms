# Vite environment variables (prefix with VITE_ to expose to client-side code)
# Copy to .env.local and fill in values for your environment.

# The base URL for the API (used for local dev without the ASP.NET Core proxy)
# VITE_API_BASE_URL=https://localhost:54186/api/v1
